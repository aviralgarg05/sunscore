version: '3.8'

services:
  sunscore:
    build: .
    container_name: sunscore-app
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
    networks:
      - sunscore-network

networks:
  sunscore-network:
    driver: bridge


